# ─────────────────────────────────────────────────────────────
# Cloudflare WORKERS erlauben in _redirects NUR relative URLs.
# Domainübergreifende 301s (.com / fxalpha.de / fxalphaebusinessconsulting.de
# -> fxconsulting.de) sind hier NICHT möglich und werden stattdessen über
# Cloudflare "Redirect Rules" im Dashboard pro Domain eingerichtet.
#
# Diese Datei bleibt bewusst ohne Regeln (nur relative Redirects wären erlaubt,
# z. B. für Legacy-Pfade auf derselben Domain — aktuell keine nötig).
# ─────────────────────────────────────────────────────────────
